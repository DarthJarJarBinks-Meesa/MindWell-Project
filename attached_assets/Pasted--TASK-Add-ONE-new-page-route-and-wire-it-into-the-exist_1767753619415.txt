
TASK: Add ONE new page/route and wire it into the existing navigation.

--------------------------------------------
NEW PAGE / ROUTE: “Brain Map”
--------------------------------------------

1) Add a new page/route at /brain-map with the title “Interactive Brain Map”.

2) On this page, render an interactive 3D brain viewer:
   - Use Three.js.
   - Use OrbitControls (rotate/zoom/pan).
   - Load a LOCAL 3D model file from:
     /assets/brain_segmented.glb
   - Show a loading state: “Loading 3D brain…”
   - If WebGL isn’t supported, show a friendly fallback message.

3) Clicking brain regions:
   - Assume the GLB is segmented so each region is a separate mesh with a meaningful mesh.name.
   - Implement raycasting so when the user clicks a mesh:
       a) highlight the mesh (change material color or emissive)
       b) display mesh.name in an info panel as “Region”
       c) display a short description from a local JS object keyed by mesh.name
   - If no description exists for that mesh.name, show:
     “Description: Not yet added.”

4) UI Layout:
   - Two-column layout on desktop:
       Left: 3D canvas
       Right: info panel (Region name + Description + optional Tip)
   - On mobile, stack vertically.

5) Controls:
   - “Reset view” button that restores the original camera position.
   - A searchable dropdown (or search input + list) populated from the description dictionary keys.
   - Selecting a region from the dropdown highlights it and moves the camera to focus on it.

6) Navbar:
   - Add a nav link labeled “Brain Map” that routes to /brain-map.
   - Do NOT change existing nav items.

7) Reliability rules (must follow):
   - Do NOT fetch any online brain assets.
   - Do NOT claim medical accuracy.
   - Keep all region descriptions local and editable in code.
   - Add clear comments explaining that the model must be segmented + mesh names should match dictionary keys.

8) Assets:
   - Ensure there is a public/static folder path so the project can load:
     /assets/brain_segmented.glb
   - If the file is missing, show a helpful message telling where to put it.

Deliver the final changes as edits to the existing codebase:
- Create the new page component/file
- Add the route
- Add the navbar link
- Add any small shared UI components only if necessary
- Do not refactor unrelated code